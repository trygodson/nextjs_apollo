import Layout from '../components/Layout';
import LoginComponent from '../components/login';

const Login = () => {
  return (
    <Layout>
      <LoginComponent />
    </Layout>
  );
};

export default Login;
